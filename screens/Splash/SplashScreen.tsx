import React, { FunctionComponent } from 'react';
import { CenteredScreen, Text } from 'components';

const SplashScreen: FunctionComponent<{}> = () => (
    <CenteredScreen>
        <Text>Splash Screen</Text>
    </CenteredScreen>
);

export default SplashScreen;
